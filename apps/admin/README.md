# Admin (fase 7)

Panel para gerencia: ventas por día/corte (`vw_corte_resumen`), inventario
por almacén (`vw_stock_almacen`), catálogo, empleados y cortes.
Se migrará desde el repo `puntodeventa` consumiendo `@shake/supabase`.
